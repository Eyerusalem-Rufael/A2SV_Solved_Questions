class Solution:
    def isPalindrome(self, x: int) -> bool:
        z = str(x)
        left = 0
        right = len(z) - 1
        while left < right:
            if z[left] == z[right]:
                left+=1
                right-=1
            else:
                return False
        return True

        
            
