class Solution:
    def sumOfThree(self, num: int) -> List[int]:
        x = num // 3
        arr = list([x-1,x,x+1])
        if sum(arr) == num:
            return arr
        else:
            return []
