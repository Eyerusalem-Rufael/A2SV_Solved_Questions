from collections import defaultdict
class Solution:
    def findDuplicates(self, nums: List[int]) -> List[int]:
        dic = defaultdict(int)
        res = []

        for i in nums:
            dic[i]+=1
            
            if dic[i] == 2:
                res.append(i)

        return res



        
