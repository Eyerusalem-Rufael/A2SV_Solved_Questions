n , k = map(int, input().split())
nums = list(map(int, input().split()))
diff = []
tot_range = nums[-1] - nums[0]
 
for i in range(len(nums)-1):
    diff.append(nums[i+1] - nums[i])
 
diff.sort(reverse=True)
ans = tot_range - sum(diff[:k-1])
 
print(ans)
