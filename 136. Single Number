from  collections import defaultdict
class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        dic = defaultdict(int)
        for i in nums:
            dic[i] +=1
        for i,elt in dic.items():
            if elt == 1:
                return i
                
        
        
